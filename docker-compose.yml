version: "3.4"
services:
  redis:
    image: "redis:latest"
    networks:
      - internal
  molinfo:
    build: .
    ports:
      - "8042:8042"
    environment:
      - MOLINFO_REDIS_HOST=redis
    networks:
      - internal
    volumes:
      - ./build/install/molinfo:/app:ro
networks:
  internal:
  external:
